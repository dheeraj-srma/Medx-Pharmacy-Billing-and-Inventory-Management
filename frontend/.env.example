# Backend API URL
# In development: leave empty (Vite proxy handles it via vite.config.ts)
# In production (Vercel frontend → Render backend): set to your Render service URL
# VITE_API_URL=https://your-backend.onrender.com/api/v1
